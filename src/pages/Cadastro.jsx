import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import AuthLayout from '../components/AuthLayout'; import Icon from '../components/Icon';
export default function Cadastro(){const navigate=useNavigate();const [showPassword,setShowPassword]=useState(false);function submit(e){e.preventDefault();navigate('/inicio');}return <AuthLayout><form className="auth-card" onSubmit={submit}><div className="field"><label htmlFor="register-email"><Icon name="mail" size={15}/> E-mail</label><div className="input-wrap"><input id="register-email" type="email" placeholder="seu@email.com" required/></div></div><div className="field"><label htmlFor="register-password"><Icon name="lock" size={15}/> Senha</label><div className="input-wrap"><input id="register-password" type={showPassword?'text':'password'} minLength="6" placeholder="••••••••" required/><button type="button" className="input-action" onClick={()=>setShowPassword(!showPassword)} aria-label="Mostrar senha"><Icon name="eye"/></button></div></div><button className="primary-btn primary-btn--full">Cadastrar</button><p className="auth-switch">Tem cadastro? <Link to="/login">Login</Link></p></form></AuthLayout>;}
