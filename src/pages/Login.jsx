import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import AuthLayout from '../components/AuthLayout'; import Icon from '../components/Icon';
export default function Login() { const navigate = useNavigate(); const [showPassword,setShowPassword]=useState(false); function submit(e){e.preventDefault();navigate('/inicio');} return <AuthLayout><form className="auth-card" onSubmit={submit}><div className="field"><label htmlFor="login-email"><Icon name="mail" size={15}/> E-mail</label><div className="input-wrap"><input id="login-email" type="email" placeholder="seu@email.com" required/></div></div><div className="field"><label htmlFor="login-password"><Icon name="lock" size={15}/> Senha</label><div className="input-wrap"><input id="login-password" type={showPassword?'text':'password'} placeholder="••••••••" required/><button type="button" className="input-action" onClick={()=>setShowPassword(!showPassword)} aria-label="Mostrar senha"><Icon name="eye"/></button></div></div><div className="auth-card__helper"><Link to="/recuperar-senha">Esqueci minha senha</Link></div><button className="primary-btn primary-btn--full">Entrar</button><p className="auth-switch">Não tem cadastro? <Link to="/cadastro">Cadastrar</Link></p></form></AuthLayout>; }
